(function(Streak) {
  var $ = Streak.jQuery,
    Gmail = Streak.Gmail,
    BB = Streak.BentoBox,
    HTML = Streak.HTML,
    Locale = Streak.Locale,
    _ = Streak._,
    Date = Streak.Date;


  var wModel = function(options){
      var self = this;
      self._delegates = [];
  };

  _.extend(wModel.prototype, {


      ddfkdsjfsdj: null
  });

  BB.Modules.wModel = wModel;
})(Streak);
