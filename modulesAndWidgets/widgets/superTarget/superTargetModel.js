(function(Streak) {
  var $ = Streak.jQuery,
    Gmail = Streak.Gmail,
    BB = Streak.BentoBox,
    HTML = Streak.HTML,
    Locale = Streak.Locale,
    _ = Streak._,
    Date = Streak.Date;


  var SuperTargetModel = function(options){
      var self = this;
      self._delegates = [];
  };

  _.extend(SuperTargetModel.prototype, {


      ddfkdsjfsdj: null
  });

  BB.Modules.SuperTargetModel = SuperTargetModel;
})(Streak);
