(function(Streak) {
  var $ = Streak.jQuery,
    Gmail = Streak.Gmail,
    BB = Streak.BentoBox,
    HTML = Streak.HTML,
    Locale = Streak.Locale,
    _ = Streak._,
    Date = Streak.Date;


  var {{replace_class}}Model = function(options){
      var self = this;
      self._delegates = [];
  };

  _.extend({{replace_class}}Model.prototype, {


      ddfkdsjfsdj: null
  });

  BB.{{replace_MorW}}.{{replace_class}}Model = {{replace_class}}Model;
})(Streak);
